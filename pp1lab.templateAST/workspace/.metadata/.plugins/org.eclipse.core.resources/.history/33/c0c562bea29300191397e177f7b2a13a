
program test2

enum IncrementType { BY_ONE, DOUBLE_VALUE }

int i;
bool ibool;

int niz[];
char niz2[];
{
	
	int sum(int z, int d)
	{
		return true;
	}
	
	int inkrement(int a, IncrementType type) int pom; 
	{
		if(type == IncrementType.BY_ONE) pom = 1;
		else if(type == IncrementType.DOUBLE_VALUE) pom = a;

		return sum(a,pom);
	}
	
	void main()
	{
		if(1!=1) print(-6);
		else print(0);  
			
		ibool = false;
		i = 0;
		for(i=0;i<5;i++)
		{
			if(i == 2) ibool = true;
		}
		
		if(ibool)
			if(i == 5) print(1); 
			else print(-1);
		
		niz = new int[3] {1, 2};

		niz2 = new char[3] {'a', 1, 'c'};
		
		
		print(eol);
		
		i = 0;
		i = inkrement(i, true);
		
		
		
	}
}

